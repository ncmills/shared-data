# Expand set — ranked engine-change greenlight list

Generated by `scripts/rank-expand-set.ts` on 2026-07-05.

This is the deferred engine-change greenlight list called for by the plan's decision to do "data + audit now, engine wiring later" (Phase 2 close-out, Task 8). It ranks the `expand` reach recorded in `docs/expand-set.json` (Task 7) — rows that are relevant to a wizard but that no engine currently reads. **Every row below is INERT.** Nothing here is live reach, and nothing is wired into any wizard by this document. Wiring is a future, separate, explicitly-greenlit task per candidate.

Source: `docs/expand-set.json` — **1124** total expand entries, aggregated below into 4 candidates.

## Effort heuristic

- **S** — the target wizard already has a working adapter for a structurally similar shared-array pattern (a site-keyed array carrying `sites`/`wizards` fields, e.g. `residencesForSite`) that this kind could be read through with a minor extension.
- **M** — the target wizard doesn't read this exact kind yet, but already reads an adjacent kind/category with an overlapping data shape (e.g. lodging-style venues) — needs a new reader, but one that follows an existing pattern.
- **L** — the target wizard has no reader touching this kind at all, and the kind is conceptually foreign to what it currently ingests (e.g. a party wizard reading golf-course data) — needs a net-new entity-kind reader plus new brand-fit logic.

"Wizards affected" counts distinct target wizards for the same entity **kind** — the unit of engine-change effort is usually "build a reader for this kind," and that reader can serve every wizard the kind expands to. Leverage = rows × wizards affected.

## Ranked candidates

| Rank | Candidate | Rows unlocked | Wizards affected | Effort | Leverage |
|---:|---|---:|---:|:---:|---:|
| 1 | residence → bestman | 328 | 2 | M | 656 |
| 2 | residence → moh | 328 | 2 | M | 656 |
| 3 | golf-destination → offsite-outing | 234 | 2 | S | 468 |
| 4 | golf-destination → offsite-retreat | 234 | 2 | S | 468 |
