{
  "name": "shared-data",
  "version": "1.2.0",
  "description": "Shared data primitives: geographic (states, regions, airports, slugify) + the tagged destination universe (party cities, golf courses, estate residences, TDF golf-trip destinations, MOH/BESTMAN locals) consumed by all wizard sites via per-wizard overlays.",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "audit": "tsx scripts/audit/index.ts",
    "reach": "tsx scripts/audit/consumer-reach.ts",
    "test": "tsx --test $(find scripts src -name '*.test.ts')",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "npm run typecheck && tsx scripts/verify-universe.ts && tsx scripts/check-brand-rules.ts && tsx scripts/audit/index.ts",
    "new-data-report": "tsx scripts/new-data-report.ts --write",
    "coord-elevations": "tsx scripts/check-coordinate-elevations.ts"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
